# @package _global_

# usage: +experiment=celeba_gender

defaults:
  - override /data: celeba/gender_smiling
  - override /bias: celeba/no_smiling_females
  - override /enc: celeba
  - override /fdm: celeba/base

data:
  context_pcnt: 0.5
