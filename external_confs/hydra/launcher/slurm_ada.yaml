#
# usage: hydra/launcher=slurm_ada
#
defaults:
  - submitit_slurm

partition: ada
cpus_per_task: 8  # on ada, we have 3 CPUs per GPU
timeout_min: 99999  # 99999 minutes = a few months

additional_parameters:
  gres: "gpu:1"  # 1 GPU
  # gres: "shard:1"  # 8 GB of GPU memory
